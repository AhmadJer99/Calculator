namespace EnumsLibrary;

    public class Enums
    {
        public enum MenuAction
        {
            Calculator,
            UsePastResult, 
            ShowHistory,
            DeleteHistory
        }

        public enum Operation
        {
            Addition = 'a',
            Subtraction = 's',
            Multiplication = 'm',
            Division = 'd'
        }
}

